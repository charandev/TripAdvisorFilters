import { OnestoppipePipe } from './onestoppipe.pipe';

describe('OnestoppipePipe', () => {
  it('create an instance', () => {
    const pipe = new OnestoppipePipe();
    expect(pipe).toBeTruthy();
  });
});
