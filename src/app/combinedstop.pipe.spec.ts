import { CombinedstopPipe } from './combinedstop.pipe';

describe('CombinedstopPipe', () => {
  it('create an instance', () => {
    const pipe = new CombinedstopPipe();
    expect(pipe).toBeTruthy();
  });
});
